diff --git a/backend/src/main/java/com/edu/portal/auth/controller/AuthController.java b/backend/src/main/java/com/edu/portal/auth/controller/AuthController.java
index 0000000..0000000 100644
--- a/backend/src/main/java/com/edu/portal/auth/controller/AuthController.java
+++ b/backend/src/main/java/com/edu/portal/auth/controller/AuthController.java
@@ -33,9 +33,15 @@ public class AuthController {
 
     @PostMapping("/cas/callback")
     public ApiResponse<LoginResponse> casCallback(@Valid @RequestBody CasCallbackRequest request) {
-        User user = userService.authenticate("teacher001", "123456");
-        return ApiResponse.ok(LoginResponse.of(safeUser(user)));
+        // SECURITY: never issue a token from an unverified CAS ticket.
+        // Fail closed until a real CAS serviceValidate integration is implemented.
+        // Final implementation should:
+        // 1. call CAS /serviceValidate with request.ticket + request.service;
+        // 2. reject non-success responses and ticket/service mismatches;
+        // 3. map the CAS principal to a local active user;
+        // 4. issue token only for the validated user.
+        throw new BusinessException(401, "INVALID_CAS_TICKET");
     }
 
     @PostMapping("/login")

# 说明：该 patch 是安全兜底建议，不是完整 CAS 集成实现。
# 它优先消除认证绕过风险；真实 CAS 登录需要另行接入 CasAuthenticationService/CAS client。
# 验收要求：无效 ticket 不得返回 200，响应不得包含 accessToken/refreshToken。
